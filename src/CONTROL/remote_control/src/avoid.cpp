#include "avoid.h"

avoid::avoid(ros::NodeHandle *n): carstate(n)
{
    // sub = n.subscribe<some::type...>("/ft", 1000);

}

void avoid::run(void)
{
    //Ackerman(_speed, 0);
}

avoid::~avoid(void)
{
    
}
