#include "path.h"

path::path(ros::NodeHandle *n): carstate(n)
{
    // sub = n.subscribe<some::type...>("/ft", 1000);

}

void path::run(void)
{
    //Ackerman(_speed, 0);
}

path::~path(void)
{
    
}
